# Config

Configurações comuns do monorepo.

Use este pacote para:

- ESLint.
- Prettier.
- TypeScript base config.
- Configurações técnicas compartilhadas.

Arquivos de ambiente devem ficar nos apps correspondentes, como `apps/web/.env.example` e `apps/api/.env.example`.

